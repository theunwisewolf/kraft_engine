#include "kraft_math.h" 

float4 RotationMatrix(float3 euler)
{
    float4 out(Identity);
    // for (int i = 0; i < n; i++)
    // {
    //     out[n][i] = position[i];
    // }
    
    return out;
}